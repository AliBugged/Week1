# Week 4 Notes

- Linear search checks elements one by one from left to right.
- Binary search works only on a sorted list.
- Binary search is faster because it cuts the search space in half.
- Selection sort finds the minimum element and puts it in the correct place.
- Insertion sort inserts the current element into the correct place in the sorted part.